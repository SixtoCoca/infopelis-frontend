import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#336B87',
    },
    background: '#f5f5f6',
    secondary: {
      main: '#2A3132',
    },
  },
});

export default theme;